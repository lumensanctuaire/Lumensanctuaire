// Réservée aux moments de passage de niveau — pas un séparateur générique.
export default function ThresholdLine() {
  return (
    <div
      style={{
        height: '1px',
        background: 'linear-gradient(90deg, #CFAE6A, transparent)',
        width: '100%',
      }}
    />
  )
}
